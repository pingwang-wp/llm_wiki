[project]
name = "llm-wiki-python"
version = "0.1.0"
description = "Python port of selected LLM Wiki TypeScript core utilities"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
